#Removes any repetative ?s,!s and .s
def removeHashtags(text):
	words = text.split(' ')

	new = []
	for word in words:
		if not word[0]=="#":
			new.append(word)

	new_text = ' '.join(new)
	return new_text



def Filter(text):
	return removeHashtags(text)


