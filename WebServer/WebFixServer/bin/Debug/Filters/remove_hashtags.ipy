#Removes #s
def Filter(text):
	words = text.split(' ')

	new = []
	for word in words:

		try:
			if not word[0]=="#":
				new.append(word)
		except:
			new.append(word)

	new_text = ' '.join(new)

	return new_text